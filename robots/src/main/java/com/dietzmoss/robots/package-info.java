/**
 * Robot agents and support types shared by every simulation.
 * <p>
 * Decision: keep all {@code Robot} subclasses and reusable helpers here so a
 * simulation app only chooses who fights and how the battle is launched.
 * Simulations must not define their own agents.
 */
package com.dietzmoss.robots;
