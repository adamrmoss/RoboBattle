/**
 * Helpers that several agents can share (heading math, later targeting, and so on).
 * <p>
 * Keep this package free of {@code Robot} subclasses — those stay in
 * {@code com.dietzmoss.robots} so Robocode's repository listing stays obvious.
 */
package com.dietzmoss.robots.support;
